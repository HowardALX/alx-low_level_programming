#include "main.h"
/**
 * _abs - func program computes absolute value of integer
 * @n: Integer to check
 * Return: Absolute value of integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
